# QA / Playwright — Vercel Deployment Protection bypass
# Project → Settings → Deployment Protection → Protection Bypass for Automation
# Keep the value empty in this file. Put the real secret only in .env (gitignored).
VERCEL_AUTOMATION_BYPASS_SECRET=

# GTM API credentials: copy automation/.env.example → automation/.env
